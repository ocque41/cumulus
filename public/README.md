# Public Assets

This directory contains static files (such as the favicon) that Next.js serves verbatim.
Keep this folder in deployments so runtime asset references continue to resolve when Vercel serves the Next.js build.
