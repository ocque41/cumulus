# Cumulus rebuild handoff — 2026-07-16

## Scope

Coordinate the fresh Cumulus reference build, public project-post research, notification-only identity, Resend delivery, responsive design verification, and the later original visual-structure branch.

## Assumptions

- The legacy checkout is user-owned and must remain untouched.
- The existing Vercel project, domain, Git linkage, and sender configuration are external and not proven by this repository.
- Local project research is read-only; only authorized, public, source-backed facts may reach published content.
- Provider credentials and private production evidence never belong in this public repository.

## Current snapshot

This snapshot was revalidated on 2026-07-17. It separates proven public-site behavior from the remaining controlled notification-provider lifecycle.

| Workstream | Snapshot | Unproven work |
| --- | --- | --- |
| Fresh Git/Vite foundation | Parentless empty root, preserved archive, React/Vite release, and existing Vercel project are proven | None for the public-site foundation |
| Brand, fonts, and dither | Supplied fonts, closed palette, code-native cloud, dither primitives, reduced-motion and fallback behavior pass | None for the implemented visual system |
| Home, work, logs, articles, and footer | 28 static routes and responsive desktop/mobile walkthroughs pass locally and on Production | None for the public reading experience |
| Project content | 24 substantial posts and a 10-project public-work directory have immutable source boundaries and backlinks | Revalidate time-sensitive claims on a future content release |
| Auth and preferences | PKCE, consent, RLS, preference, sign-out, focus, error, and privacy-disclosure tests pass | Controlled Production magic-link receipt and SMTP/rate-limit inspection |
| Notification backend | Lease/retry/idempotency, unsubscribe, signed webhook suppression, accessible email rendering, and production fail-closed configuration pass | Truthful postal address plus controlled receipt, duplicate, unsubscribe, bounce/complaint, and later-suppression proof |
| Planning, agents, and branches | Matrix, handoff, reusable skill, selected branch, and literal branch are current; runtime trees differ only in `src/styles.css` | Keep the external notification evidence partial until it is actually observed |

## Public artifacts added by this handoff

- `planning/README.md`
- `planning/implementation-plan.md`
- `planning/source-evidence-policy.md`
- `planning/design-verification-checklist.md`
- `planning/notification-vercel-safety-gates.md`
- `planning/requirement-evidence-matrix.md`
- `agents/README.md`
- `agents/third-party-skills.md`
- `agents/handoffs/2026-07-16-rebuild.md`
- `skills/project-post-research/SKILL.md`
- `skills/project-post-research/agents/openai.yaml`

## Safety record

The initial planning phase performed no provider mutation or publication. Later approved pushes and production inspections are recorded in the completion matrix without credential values, subscriber data, provider IDs, or private project detail.

## Checks run

- Repository skill validation: passed (`Skill is valid!`).
- Relative Markdown link check across 22 public documentation files: passed, 31 links checked.
- Post-ledger parity check: passed for 24 published slugs, 24 unique rows, every final source URL, and a 700–767 word range.
- Focused content suite: passed, 14/14 tests.
- `npm run license:check`: passed for the repository's declared Apache, OFL, MIT component, and Paper Shaders boundaries. This check does not cover the newly inventoried agent skills; their standalone upstream license text remains a public-release gate.
- Targeted private-path and credential-pattern scan of the new artifacts: passed.
- `npm run security:scan`: passed (`PUBLIC_SAFETY_OK`) across 117 public files and four browser assets.

## Required next action

The implementation and design branches are reconciled. The runtime tree introduced by `main@e1d0328` is READY on `cumulush.com`; `request/cumulus-original@c3baf3d` is runtime-identical and `request/jacquard-reference@281fe37` differs only in `src/styles.css`. All three runtime candidates pass 153 tests, 28 generated routes, the complete release gate, and 13 responsive browser checks with one intentional desktop skip. The official Resend skills remain installed outside Git, and the authenticated webhook is enabled for bounced, complained, and suppressed events. Its signing secret entered Vercel Production without entering Git or a regular file; an unsigned live probe receives `400 invalid_webhook`.

The remaining external gate is notification-only: obtain the truthful sender postal address, configure `NOTIFICATION_POSTAL_ADDRESS`, verify the Supabase Auth SMTP/rate-limit controls, and use a controlled inbox to prove magic-link receipt, explicit activation, notification receipt, duplicate-publish idempotency, browser and one-click unsubscribe, bounce/complaint handling, and later suppression. Do not use an arbitrary reader or infer a legal address.
